{
  "name": "@edgecommons/edge-console-protocol",
  "version": "0.1.0",
  "description": "Shared Edge Console types: the WS API contract (fleet snapshots, delta events, liveness) and the UNS envelope shapes the server and UI exchange.",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  }
}
