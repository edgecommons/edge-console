{
  "name": "@edgecommons/edge-console-server",
  "version": "0.1.0",
  "description": "Edge Console server: a edgecommons TypeScript component that ingests the site UNS bus (six class wildcards + the bridge raw-LWT path) into the FleetModel - the timestamped last-known-value cache with console-side miss-detection. WS gateway (C2), CommandGateway (C4) land in later slices.",
  "license": "Apache-2.0",
  "main": "dist/main.js",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "dependencies": {
    "@edgecommons/edge-console-protocol": "^0.1.0",
    "@edgecommons/edgecommons": "^0.2.0",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/ws": "^8.18.1",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "^5",
    "vitest": "^4.1.9"
  }
}
